TOKEN=your_discord_bot_token
CHANNELID=your_discord_channel_id
CRONTIME=0 9 * * *           # Cron format (this sends daily at 9 AM)
FIRSTINDEX=0                   # Start index in the data array
LASTINDEX=42                   # Last index (after which bot stops)